<%= form_for(@route) do |f| %>
  <% if @route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% @route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :route_id %><br>
    <%= f.text_field :route_id %>
  </div>
  <div class="field">
    <%= f.label :routeName %><br>
    <%= f.text_field :routeName %>
  </div>
  <div class="field">
    <%= f.label :printSequence %><br>
    <%= f.number_field :printSequence %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
